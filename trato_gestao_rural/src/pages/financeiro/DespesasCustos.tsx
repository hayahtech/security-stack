import { Receipt } from "lucide-react";
import { PlaceholderPage } from "@/components/PlaceholderPage";
export default function DespesasCustos() {
  return <PlaceholderPage title="Despesas & Custos" icon={Receipt} />;
}
